proj_line_x = DashedLine(
            start=target_point, 
            end=x_axis_point, 
            color=GRAY_A, 
            dash_length=0.1
        )
        proj_line_y = DashedLine(
            start=target_point, 
            end=y_axis_point, 
            color=GRAY_A, 
            dash_length=0.1
        )

        # 3. Label for the vertex
        vertex_dot = plotted_dots[3]
        vertex_label = MathTex(r"\text{Vertex } (3, 0.5)", color=YELLOW, font_size=20)
        vertex_label.next_to(vertex_dot, DOWN, buff=0.2)

        # --- ANIMATION SEQUENCE ---
        
        # A. Highlight the table row and pulse the dot on the plane simultaneously
        self.play(
            Create(table_box),
            vertex_dot.animate.scale(1.5).set_color(YELLOW),
            run_time=0.8
        )
        
        # B. Extend dashed projection lines from the point down/left to the axes
        self.play(
            Create(proj_line_x),
            Create(proj_line_y),
            Write(vertex_label),
            run_time=1.0
        )
        self.wait(2)
